name: kipoi-dev
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - git-lfs>=3.2.0
  - bedtools>=2.30.0
  - pytorch>=1.11.0
  - pytest>=7.1.2
  - pytest-cov>=3.0.0
  - coveralls>=3.3.1
  - tensorflow>=2.8
  - keras>=2.8
  - pyarrow>=8.0
  - zarr>=2.11.3
  - pip>=21.2.4
  - pip:
    - protobuf==3.20
    - sklearn>=0.0
    - pybedtools>=0.9.0
    - kipoi-interpret
    - kipoiseq
    - sorted-nearest==0.0.33

